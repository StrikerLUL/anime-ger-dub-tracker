# Abhängigkeiten für den GitHub-Actions-Workflow.
# Die Zusatzquellen (AniList, MyAnimeList, News-Feeds) laufen bewusst mit der
# Standardbibliothek – hier steht deshalb nur, was wirklich nötig ist.

playwright>=1.48.0      # anisearch.de rendert die Listen per JavaScript
pytest>=8.0.0           # Datenlogik-Tests vor jedem Lauf
